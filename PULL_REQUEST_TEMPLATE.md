# Pull Request

## Summary
Adds dark mode toggle and persistent mobile bottom navigation. Dark mode uses the site's current color palette and is toggled via a button in the header. Mobile users get a bottom navigation bar for quick access to main sections. Includes updates to header, footer, main CSS, and a new JavaScript file for theme toggling.